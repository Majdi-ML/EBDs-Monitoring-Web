<form method="post" action="{{ path('app_log_files_delete', {'id': log_file.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ log_file.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
