<form method="post" action="{{ path('app_serveurs_delete', {'id': serveur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ serveur.id) }}">
    <button class="btn">Delete</button>
</form>
