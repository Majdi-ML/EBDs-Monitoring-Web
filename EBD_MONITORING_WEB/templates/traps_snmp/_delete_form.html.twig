<form method="post" action="{{ path('app_traps_snmp_delete', {'id': traps_snmp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traps_snmp.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
