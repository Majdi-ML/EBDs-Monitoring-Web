<form method="post" action="{{ path('app_log_files_patterns2_delete', {'id': log_files_pattern.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ log_files_pattern.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
